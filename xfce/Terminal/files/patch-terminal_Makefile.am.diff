--- terminal/Makefile.am.orig	2010-05-21 16:09:57.000000000 -0400
+++ terminal/Makefile.am	2014-05-12 20:02:22.000000000 -0400
@@ -1,4 +1,4 @@
-INCLUDES = \
+AM_CPPFLAGS = \
 	-DDATADIR=\"$(datadir)\" \
 	-DHELPDIR=\"$(docdir)\" \
 	-DEXO_DISABLE_DEPRECATED\
@@ -88,10 +88,11 @@
 	$(VTE_LIBS) \
 	$(TERMINAL_LIBS)
 
-# install symlink to 'terminal'
+# install symlink to 'terminal' (should fail on case-insensitive
+# filesystems):
 install-exec-hook:
 	$(mkinstalldirs) $(DESTDIR)$(bindir)
-	-( cd $(DESTDIR)$(bindir) ; test -x terminal || test -x Terminal && ln -sf Terminal terminal )
+	-( cd $(DESTDIR)$(bindir) ; test -x terminal || (test -x Terminal && ln -sv Terminal terminal) )
 
 if HAVE_DBUS
 Terminal_CFLAGS += \
@@ -106,8 +107,8 @@
 ## Rules to auto-generate built sources
 ##
 ## This is a bit tricky with automake, and non-trivial to implement. The
-## rules below seem to work fine and don't seem to break the build, but
-## they are only enabled in maintainer mode, so arbitrary users don't get
+## rules below seem to work fine and do NOT seem to break the build, but
+## they are only enabled in maintainer mode, so arbitrary users do NOT get
 ## trapped in automake's oddities. Therefore we ship the autogenerated
 ## files as part of the dist tarball.
 ##
